# 人生グラフ 要件定義・技術選定

## 1. プロダクト概要

「人生グラフ」は、ユーザーが人生の出来事と、その時々の感情・充実感の上下を一本の折れ線グラフとして作成し、画像または URL で共有できる Web アプリ。

- 横軸: 日付を基礎とする連続時間軸。人生全体では年齢を主表示し、設定により西暦を併記する
- 縦軸: 「人生の波」（表示値名: 「実感スコア」）を -100〜+100 で表す
- イベント: 年齢または日付、タイトル、説明、スコアを持つ
- 初期状態: スコア 0 の水平線
- 主操作: グラフ上の任意地点をダブルクリック／ダブルタップ相当の操作でイベントを追加する

「幸福度」は悲喜以外の転機を扱いにくく、「テンション」は長期的な人生評価として軽すぎるため、UI 上は中立的な「人生の波」を推奨する。入力値の説明には「その時期を、今振り返ってどのように感じるか」を使う。

## 2. 対象と利用シーン

### 想定ユーザー

- 自分史を気軽に可視化したい人
- SNS やブログで人生の転機を共有したい人
- 振り返り、自己紹介、キャリア棚卸しに使いたい人

### 代表的な利用フロー

1. 「人生全体」「1年間」「自由期間」から年表の期間を設定する
2. グラフをダブルクリックして出来事を追加する
3. イベントを編集・移動し、人生の波を整える
4. タイトル、配色、表示項目を調整する
5. PNG 画像または共有 URL として公開する

## 3. MVP 要件

### 3.1 年表設定

- 年表タイトルを設定できる
- 年表タイトルの初期値は「わたしの人生グラフ」とし、ユーザーが変更できる
- 人生全体モードは0歳から始まり、任意の終了年齢（1〜120歳）まで表示できる
- 初期設定は1996年生まれとし、0歳から現在の日付までを記録可能範囲として表示する
- 初期状態から0歳・スコア0の「誕生」イベントを表示する
- 生年月日を設定でき、変更時は「誕生」イベントと現在年齢を自動更新する
- 生年月日はカレンダーから選択でき、編集中は空欄を許容する
- 現在の日付より右側に、ラベルや線を見やすくするための表示専用バッファを設ける
- 初期設定では表示専用バッファや未来の日付へ出来事を追加できない
- 西暦を併記する場合は生まれ年を設定できる
- 生まれ年と終了年齢はトップ画面から直接変更できる
- 生まれ年を変更した場合、終了年齢をその年生まれの現在年齢へ自動更新する
- 開始年齢は0歳固定のため設定項目として表示しない
- 表示モードを選択できる
  - 人生全体: 生年月日／生年と終了年齢から期間を決める
  - 1年間: 対象年の1月1日〜12月31日を表示する
  - 自由期間: 任意の開始日と終了日を指定する
- 表示範囲を 0 歳から任意の終了年齢まで設定できる（初期値 100 歳、1〜120 歳）
- 年齢目盛りの間隔を表示幅に応じて自動調整する
- 生年設定済みの場合、西暦の併記をオン／オフできる
- 生まれ年から、各年齢に対応する西暦を算出する

### 3.2 グラフ

- Y 軸は -100〜+100 で固定する
- 0 の基準線を強調表示する
- イベント間を折れ線で接続する
- イベントがない区間は 0 として表示する
- グラフ上のポインター位置に対応する年齢・西暦・スコアを表示する
- ホイール／ピンチ操作およびズームボタンで時間軸を拡大・縮小できる
- ドラッグ／スワイプで表示期間を左右に移動できる
- グラフ背景を左クリックでドラッグして表示期間を左右へ移動できる
- 全期間へ戻るボタンを提供する
- 表示範囲に応じて目盛りを年、月、週、日に自動で切り替える
- 現在の目盛り単位と表示期間を常に表示する
- 年単位では年齢を主表示し、月以下では日付を主表示する。生年月日がある場合は年齢も補助表示する
- 週の開始曜日はロケールに従い、初期リリースの日本語表示では月曜日とする
- 過度なズームによる操作ミスを避けるため、最小表示範囲は7日とする
- PC とスマートフォンの表示幅に追従する
- 表示密度が高い場合も、ラベルの重なりを可能な範囲で回避する

### 3.3 イベント作成・編集

- PC: グラフ上のダブルクリックで追加ダイアログを開く
- タッチ端末: ダブルタップだけに依存せず、「イベント追加」ボタンと長押しでも追加できる
- クリック位置から日時とスコアの初期値を算出する
- 書き込み単位を年・月・日から選択でき、初期値は年とする
- 年単位では年、月単位では年月、日単位では日付として出来事を入力・移動する
- マウスホバーの日付表示と縦ガイドも選択中の書き込み単位へスナップする
- 年・月単位の点とホバーガイドは、それぞれの期間の左端ではなく中央へ配置する
- 時間軸の補助線は表示期間と表示幅から適切な本数・間隔を自動計算する
- イベントは次の項目を持つ
  - ID
  - 日付（必須。生年しか分からない場合は精度を年として保持可能）
  - 時刻（任意。MVPでは日単位までを主対象とする）
  - 日付精度（年／月／日）
  - 年齢（生年月日から算出して表示し、直接は保存しない）
  - スコア（-100〜+100、10刻み）
  - タイトル（必須、最大 60 文字）
  - 説明（任意、最大 500 文字）
  - ラベル位置（自動／上／下）
- 点のドラッグで日時・スコアを変更できる。日付は現在の目盛り単位へスナップし、修飾キーでスナップを解除できる
- 点の選択から編集・削除できる
- 同じ日時に複数イベントを登録できる
- 操作の取り消し・やり直しができる（少なくとも直近 20 操作）

### 3.4 保存

- 入力中のデータをブラウザの localStorage に自動保存する
- JSON ファイルとしてエクスポート／インポートできる
- ローカル編集はアカウント不要とし、クラウド保存・端末間同期は初期MVPには含めない
- 保存失敗、壊れた JSON、仕様バージョン不一致をユーザーに通知する

### 3.5 共有

- グラフを PNG として書き出せる
- 共有用データを圧縮して URL のハッシュ部分に格納し、リンクをコピーできる
- 共有URLの生成・閲覧ではDBやAPIを呼び出さず、アクセス増加がバックエンド負荷を発生させない
- URLには最大サイズを設け、超過時はイベント説明を除外した軽量リンク、JSON、PNG共有を案内する
- 共有 URL を開いた初期状態は閲覧専用とし、「自分用にコピーして編集」で複製できる
- URL に個人情報が含まれ得ることと、リンクを知る人が閲覧できることを共有前に明示する
- 長すぎて安全に URL 化できないデータは JSON／PNG 共有を案内する

## 4. MVP 対象外（将来候補）

- ログイン、クラウド同期、公開ギャラリー
- DBへ保存する短縮共有URLと動的OGP画像
- ユーザーアカウントに紐づく共有履歴管理
- 複数ユーザーでの共同編集
- コメント、いいね、フォロー
- 写真・動画のアップロード
- AI による文章生成や感情分析
- 複数系列（仕事、健康、恋愛など）の同時表示
- PDF／SVG 出力

初期版は完全な静的アプリとし、DB、API、サーバーレスFunctionを利用しない。共有URL自体にデータを持たせることで、アクセス集中時もオリジンサーバーやDBへの負荷と従量課金を発生させない。

## 5. 非機能要件

### アクセシビリティ

- キーボードだけでイベント追加・選択・編集・削除ができる
- グラフと同じ内容を編集可能なイベント一覧でも提供する
- 色だけで正負や選択状態を伝えない
- フォーカス表示と十分なコントラストを確保する
- `prefers-reduced-motion` を尊重する

### デザイン

- 装飾を抑え、グラフとイベントを視覚上の主役にする
- 初期画面に長い説明文を置かず、空のグラフ内に短い操作ヒントを1つだけ表示する
- 操作名はアイコンだけにせず、短いラベルまたはアクセシブルネームを付ける
- 詳細説明は初回ガイド、ツールチップ、ヘルプへ退避し、通常操作中は表示しない
- ライト、ダーク、端末設定に従う「自動」の3テーマを提供する
- 初回は「自動」とし、ユーザーの選択をlocalStorageへ保存する
- 色、余白、罫線、影はCSS Custom Propertiesのデザイントークンで管理する
- ダークテーマは単純な色反転にせず、グラフ線、0基準線、グリッド、正負の色について十分なコントラストを個別に定義する

### プライバシー・安全性

- 初期版はデータを外部サーバーへ送信しない
- HTML として入力値を直接挿入せず、XSS を防ぐ
- URL 共有には秘密保持機能がないことを明示する
- 公開前に住所、病歴、実名等の入力へ注意を促す

### 性能・品質

- 通常ケース 200 イベントまで快適に操作できる
- 主要操作に対する初期描画を一般的なスマートフォンで 2 秒以内を目標とする
- 最新 2 世代の Chrome、Edge、Firefox、Safari を対象とする
- スマートフォンは幅 360px 以上を対象とする

### SEO／共有表示

- サービス名は「人生グラフ」とする
- 編集画面の文書タイトルは「人生グラフ」とする
- 共有画面では年表タイトル（初期値「わたしの人生グラフ」）を主見出しにし、サービス名は控えめに表示する
- 共有画面の文書タイトルは「{年表タイトル} | 人生グラフ」とする
- 基本の title、description、OGP を設定する
- URL ハッシュ内の内容は OGP の動的生成に使えないため、共有カードはサービス共通画像とする

## 6. データモデル案

```ts
type TimelineDocument = {
  schemaVersion: 1;
  id: string;
  title: string;
  mode: "lifetime" | "year" | "custom";
  birth: { precision: "year" | "month" | "date"; value: string } | null;
  range: { start: string; end: string };
  endAge: number | null;
  showCalendarYear: boolean;
  theme: "light" | "dark" | "auto";
  events: TimelineEvent[];
  updatedAt: string;
};

type TimelineEvent = {
  id: string;
  occurredAt: string;
  datePrecision: "year" | "month" | "day";
  score: number;
  title: string;
  description: string;
  labelPosition: "auto" | "above" | "below";
};
```

日時を正規データとし、年齢は生年月日との差から表示時に算出する。年齢と日時の二重保持による不整合や、月・日へのズーム時の丸め誤差を避ける。日付だけのイベントはタイムゾーン変換で日がずれないよう、UTC日時ではなく `YYYY-MM-DD` の暦日として扱う。

## 7. 技術スタック

### 採用案

- 言語: TypeScript
- UI: React
- ビルド: Vite
- グラフ: D3.js（スケール、軸、ポインター座標計算、ドラッグ）+ SVG
- 状態管理: React の `useReducer` + Context
- 入力検証: Zod
- 永続化: localStorage
- URL 圧縮: `lz-string`（URL-safe 圧縮）
- 日付計算: date-fns
- PNG 出力: SVG を Canvas に描画して出力（必要なら `html-to-image` を比較検証）
- スタイル: CSS Modules または通常の CSS + CSS Custom Properties
- テスト: Vitest + React Testing Library
- E2E: Playwright
- 静的解析: ESLint + Prettier
- デプロイ: GitHub Actions + GitHub Pages、またはCloudflare Pages（同じ `dist` を配信可能にする）

### 選定理由

- React は編集ダイアログ、設定パネル、一覧、Undo/Redo など状態の多い UI を分割しやすい
- D3 を描画全体の管理に使わず、座標・軸・ドラッグ計算に限定して SVG を React で描画すると保守しやすい
- SVG は軸、ラベル、イベント点、アクセシビリティ、画像出力との相性がよい
- Vite の静的ビルドは GitHub Pages に載せやすく、サーバーを必要としない
- MVP 規模では Redux 等を入れず、履歴を持つ reducer で Undo/Redo まで扱える
- バックエンドを持たないため、共有リンクの閲覧数が増えてもDB接続数やAPI実行数は増えない

### 見送る案

- Next.js: SSR／API が不要で、GitHub Pages の静的配信では利点が小さい
- Chart.js／Recharts: 基本グラフは速く作れるが、任意位置での追加、点のドラッグ、二段 X 軸、ラベル配置など独自インタラクションで制約が出やすい
- DB／BaaS／サーバーレスFunction: 無料枠超過、書き込み荒らし、運用監視を避けるため初期版では採用しない
- Tailwind CSS: 小規模な固有デザインでは通常 CSS の方が依存と記述規約を抑えやすい

## 8. 画面構成

- ヘッダー: 「人生グラフ」、保存状態、新規、読込、共有
- 設定パネル: タイトル、表示モード、期間、生年／生年月日、終了年齢、西暦表示、テーマ
- メイン: 人生年表グラフ
- イベント一覧: 日時順のイベント。モバイルではグラフ下に配置
- イベントダイアログ: タイトル、説明、日付／年齢、日付精度、スコア、ラベル位置
- 共有ダイアログ: PNG 保存、URL コピー、JSON 保存、プライバシー注意

## 9. 操作上の重要判断

- ダブルクリックは発見しにくいため、初回ガイドと常設の「イベント追加」ボタンを併設する
- モバイルのダブルタップはブラウザのズーム等と競合するため、唯一の追加手段にしない
- イベントがない始点・終点を内部的にスコア 0 として描画するが、ユーザーイベントとして保存しない
- 同じ日時に複数イベントがある場合は同じ X 座標に置き、一覧からも確実に選択できるようにする
- 公開リンクを開いた際に localStorage を自動上書きしない
- ズームは表示だけを変え、保存されたイベント日時やスコアを変更しない
- ズーム中の追加は表示粒度に応じて年／月／日へスナップする
- 初回の空グラフには「ダブルクリックして出来事を追加」の一文だけを表示し、イベント追加後は消す
- 主要操作はグラフ、イベント追加、Undo/Redo、共有に絞り、低頻度の設定は設定パネル内へまとめる

## 10. 受け入れ基準（MVP）

- 空の年表が 0 の水平線として表示される
- グラフのダブルクリック位置に概ね対応した年齢・スコアでイベントを追加できる
- 追加、編集、ドラッグ、削除、Undo、Redo がグラフと一覧へ即時反映される
- 生年設定と西暦表示オンで、年齢目盛りの下に正しい西暦が表示される
- ズーム操作により年・月・週・日の目盛りが切り替わり、パン後もイベント位置が正しい
- 1年間モードで日単位のイベントを複数登録できる
- 再読込後もローカル保存内容が復元される
- JSON の入出力で内容が維持される
- URL 共有から同じ年表が閲覧専用で復元される
- PNG にタイトル、軸、折れ線、イベントラベルが含まれる
- キーボードとイベント一覧だけでも全編集操作を完了できる
- GitHub Pages のサブパス配下で、直接アクセスと再読込が正常に動作する
- ライト、ダーク、自動テーマが切り替わり、再読込後も選択が維持される
- ネットワークAPIを利用せず、オフライン状態でも作成・編集・URL生成・PNG出力ができる

## 11. 実装順序案

1. Vite + React + TypeScript の基盤と、GitHub Pages／Cloudflare Pagesへ移植可能な静的配信
2. データモデル、reducer、ローカル保存、単体テスト
3. SVG グラフ、レスポンシブ軸、年齢／西暦表示
4. イベント追加・編集・削除・ドラッグ、一覧、Undo/Redo
5. JSONとURL内包共有の入出力
6. PNG 出力、アクセシビリティ、E2E、表示調整

## 12. MVP 後に評価する指標

- 初回訪問から最初のイベント追加までの到達率
- 作成された年表あたりのイベント数
- PNG／URL／JSON の共有・出力率
- モバイルでのイベント追加完了率
- 入力開始から共有までに要する時間
